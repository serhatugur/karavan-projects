- route:
    id: erp-products-route
    from:
      id: from-7e8f
      uri: direct
      parameters:
        name: erp-products-route
      steps:
        - removeHeaders:
            id: removeHeaders-6fd0
            pattern: CamelHttp*
        - to:
            id: to-ed50
            uri: http
            parameters:
              httpUri: http://host.docker.internal:4004/odata/v4/simple-erp/Products
              httpMethod: GET
              authUsername: "{{env:ERP_USER}}"
              authPassword: "{{env:ERP_PASSWORD}}"
              authMethod: Basic
        - unmarshal:
            id: unmarshal-1402
            json:
              id: json-87c6
        - setBody:
            id: setBody-1555
            expression:
              simple:
                id: simple-14da
                expression: ${body[value]}
        - marshal:
            id: marshal-ef6b
            json:
              id: json-823c
